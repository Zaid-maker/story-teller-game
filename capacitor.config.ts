import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.tinypenguinskid',
  appName: 'tiny-penguin-skid',
  webDir: 'dist'
};

export default config;
